# Node
node_modules/
npm-debug.log*
*.tgz

# Certior Guard writes these into a user's repo — never commit them
certior.yml
.certior/

# Local Claude Code state (plugin repo)
.claude/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
